spring.application.name=JobApp

# Let Railway (or any host) provide the port; fallback to 8080 locally
server.port=${PORT:8080}

# Database: prefer Railway-provided env vars; fall back to your local Postgres
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/postgres}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:anshu@2427}

spring.jpa.hibernate.ddl-auto=update

# Basic (dev) user - consider removing in production
spring.security.user.name=${SPRING_SECURITY_USER_NAME:telusko}
spring.security.user.password=${SPRING_SECURITY_USER_PASSWORD:1234}
